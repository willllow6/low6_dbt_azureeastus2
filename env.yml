env_config:
  default_environment: dev
  environments:
    - name: dev
      env:
        DBT_SCHEMA: "{{ select 'dbt_' || lower(current_user()) }}"
    - name: prod
      env:
        DBT_SCHEMA: "analytics"